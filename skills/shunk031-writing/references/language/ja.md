# Japanese

Run `natural-japanese quick` after the whole-document reading pass, then apply the sanitize pass and edit any remaining local findings. Open a report with the answer a first-time reader needs. Keep one register throughout. Use 体言止め for compact support under a claim, not for the document's main reasoning.

Write a word with an established Japanese equivalent in Japanese, such as `実装` rather than `インプリメンテーション`. Introduce a technical term at first use with a Japanese explanation followed by the English term in parentheses, such as `検索拡張生成 (retrieval-augmented generation)`.

Owner-authored specimen: 「回転より増幅の寄与が大きい。」

Source: private lab notes, not publicly linkable.
