{
  "skill_name": "shunk031-python-uv-workflow",
  "cases": [
    {
      "id": "python-feature-test-first-uv",
      "prompt": "Starting from an empty checkout, create a minimal Python project for a `slugify` feature. Make the sequence observable in your work: first create a failing regression test for `slugify(\"Hello, World!\") == \"hello-world\"`; next create the implementation; then run the focused tests through the project's uv workflow and report each result in order. Make the files rather than only describing them.",
      "should_trigger": true
    },
    {
      "id": "python-fixture-path-relocation",
      "prompt": "Implement a Python utility in `src/fixture_loader.py` that opens `tests/fixtures/config.json`, and add a test for it. The repository will be relocated between worktrees, so the code must work regardless of the process's current working directory. An explicitly supplied root argument or environment variable is allowed if the implementation needs an externally selected root. Implement and test the change, then report the path strategy and result.",
      "should_trigger": true
    },
    {
      "id": "python-refactor-precommit-gate",
      "prompt": "Starting from an empty checkout, create `src/csv_reader.py` with this supplied initial function: `def parse_header(header: str | None) -> list[str]:\\n    return header.split(',') if header else []`. Then add a regression test that a missing header raises `ValueError`, change the parser to satisfy it, create the minimal project and pre-commit configuration needed for the check, install the hooks after dependency setup, run the pre-commit checks, and report the ordered results. Make the files rather than only describing them.",
      "should_trigger": true
    },
    {
      "id": "markdown-documentation-edit",
      "prompt": "Without changing files, write a three-sentence description of a fictional bookstore's opening hours for a non-Python README draft. Return only the prose draft.",
      "should_trigger": false
    },
    {
      "id": "git-branch-short-answer",
      "prompt": "In one sentence, explain what a Git branch is.",
      "should_trigger": false
    }
  ]
}
